"""Gradio GUI for playing Blokus Duo against the trained AI.

``render`` and ``game_io`` are import-light (NumPy / Torch only) and tested;
``app`` is the Gradio wrapper and is imported only when launching the app.
"""
